[Unit]
Description=Starts hierbautberlin when server is rebooted
After=network.target

[Service]
Type=simple
User=root
ExecStart=/home/deploy/hierbautberlin/start_after_reboot.sh

[Install]
WantedBy=multi-user.target